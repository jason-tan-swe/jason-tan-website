"use client";
import { Fade } from "react-awesome-reveal";

export default function Layout({ children }: any) {
    return (
        // <main className="w-full h-full overflow-auto flex flex-col gap-4 text-start">
        children
        // </main>
    );
}
